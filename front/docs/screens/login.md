# 画面名
ログイン

# 必須構成

## 1. 画面概要
ユーザーがログインするための画面です。ログインIDとパスワードを入力し、ログインボタンをクリックすることで、ログインを行うことができます。

## 2. URL
`/login`

## 3. フォーム項目・表示要素
- ログインID (テキスト入力)
- パスワード (パスワード入力)
- ログインボタン

## 4. 初期表示・デフォルト値
- ログインID、パスワードの入力欄は空の状態で表示されます。

## 5. ユーザー操作
1. ログインIDとパスワードを入力する
2. ログインボタンをクリックする

## 6. バリデーション・エラーハンドリング
- ログインIDとパスワードは必須入力です。
- 入力値が不正な場合、エラーメッセージを表示します。
- ログイン処理中は、ログインボタンをdisabledにして、ユーザーの操作を制限します。
- ログイン処理に失敗した場合、エラーメッセージを表示します。

## 7. 画面遷移
- ログイン成功時は、`/mypage`に遷移します。
- 新規アカウント登録は、`/signup`に遷移できます。

## 8. 使用しているコンポーネント
- `LoginPage`: ログイン画面のメインコンポーネント
- `LoginForm`: ログインフォームのコンポーネント
- `Button`: ログインボタンのコンポーネント

## 9. 使用しているHooks / Stores / API / Schema
- `useLoginForm`: ログインフォームのカスタムフック
- `useAuthStore`: ユーザー認証に関するストア
- `fetchUserData`: ユーザー情報を取得するAPI
- `createLogin`: ログイン処理を行うAPI
- `NewLogin`, `NewLoginSchema`: ログイン情報に関するスキーマ

## 10. 補足・制約
- ログイン・新規ユーザー登録は、午前9時から午後9時の間のみ利用可能です。

<!-- META -->
- 画面ID: login
- 最終更新日: 2026-02-22
<!-- /META -->